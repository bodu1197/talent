# ============================================
# Development Environment Variables
# ============================================
# 로컬 개발 환경에서 사용할 환경 변수
# .env.local 파일을 생성하여 실제 값을 입력하세요
#
# ⚠️ 보안 주의사항:
# - .env.local은 절대 Git에 커밋하지 마세요
# - NEXT_PUBLIC_ 접두사가 붙은 변수는 클라이언트에 노출됩니다
# - 민감한 키(SERVICE_ROLE_KEY, API_SECRET)는 서버에서만 사용하세요

# --------------------------------------------
# Supabase 설정
# --------------------------------------------
# Supabase 프로젝트의 데이터베이스 및 인증 설정
#
# 📌 NEXT_PUBLIC_SUPABASE_URL
#    - Supabase 프로젝트 URL
#    - 위치: Supabase Dashboard > Settings > API > Project URL
#    - 형식: https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# 📌 NEXT_PUBLIC_SUPABASE_ANON_KEY
#    - 공개 API 키 (클라이언트에서 사용 안전)
#    - RLS(Row Level Security) 정책을 적용받음
#    - 위치: Supabase Dashboard > Settings > API > anon/public key
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# 📌 SUPABASE_SERVICE_ROLE_KEY
#    - ⚠️ 절대 클라이언트에 노출하지 마세요!
#    - RLS를 우회하므로 서버 사이드에서만 사용
#    - 위치: Supabase Dashboard > Settings > API > service_role key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# --------------------------------------------
# PortOne (구 아임포트) 결제 설정
# --------------------------------------------
# 결제 처리를 위한 PortOne 설정
#
# 📌 NEXT_PUBLIC_PORTONE_STORE_ID
#    - PortOne 상점 ID
#    - 개발: store-test-xxxxx (테스트 키 사용)
#    - 프로덕션: store-xxxxx (실제 키 사용)
#    - 위치: PortOne 콘솔 > 상점 관리
NEXT_PUBLIC_PORTONE_STORE_ID=your-store-id

# 📌 PORTONE_API_SECRET
#    - ⚠️ 절대 클라이언트에 노출하지 마세요!
#    - 서버에서 결제 검증 시 사용
#    - 위치: PortOne 콘솔 > API 키 관리
PORTONE_API_SECRET=your-api-secret

# 📌 NEXT_PUBLIC_PORTONE_CHANNEL_KEY
#    - 결제 채널 키
#    - 클라이언트에서 결제 모듈 초기화 시 사용
#    - 위치: PortOne 콘솔 > 채널 관리
NEXT_PUBLIC_PORTONE_CHANNEL_KEY=your-channel-key

# --------------------------------------------
# Sentry 에러 모니터링 (선택사항)
# --------------------------------------------
# 프로덕션 환경에서 에러 추적 및 모니터링
#
# ℹ️ Sentry 설정이 없어도 앱은 정상 작동합니다
# next.config.js에서 조건부로 활성화됩니다
#
# 📌 NEXT_PUBLIC_SENTRY_DSN
#    - Sentry 프로젝트의 DSN (Data Source Name)
#    - 위치: Sentry > Settings > Client Keys (DSN)
#    - 형식: https://xxxxx@sentry.io/xxxxx
NEXT_PUBLIC_SENTRY_DSN=your-sentry-dsn

# 📌 SENTRY_ORG
#    - Sentry 조직 이름
#    - 소스맵 업로드 시 필요
SENTRY_ORG=your-org

# 📌 SENTRY_PROJECT
#    - Sentry 프로젝트 이름
#    - 소스맵 업로드 시 필요
SENTRY_PROJECT=your-project

# 📌 SENTRY_AUTH_TOKEN
#    - ⚠️ 빌드 시에만 사용 (런타임에는 불필요)
#    - 소스맵 업로드를 위한 인증 토큰
#    - 위치: Sentry > Settings > Auth Tokens
SENTRY_AUTH_TOKEN=your-auth-token

# --------------------------------------------
# 플랫폼 무통장 입금 계좌 정보
# --------------------------------------------
# 사용자가 무통장 입금 시 표시할 계좌 정보
#
# 📌 개발 환경에서는 테스트 계좌 정보 사용
# 📌 프로덕션에서는 반드시 실제 계좌로 변경
NEXT_PUBLIC_BANK_NAME=은행명
NEXT_PUBLIC_BANK_ACCOUNT=계좌번호
NEXT_PUBLIC_BANK_HOLDER=예금주명

# --------------------------------------------
# 번들 분석 (선택사항)
# --------------------------------------------
# 빌드 시 번들 크기 분석
#
# 📌 사용법: ANALYZE=true npm run build
# ANALYZE=false
